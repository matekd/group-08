<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <!-- This button is a white space filler which all other buttons align around -->
    <Button
        android:id="@+id/whitespace"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:alpha="0"/>

    <!-- Forward button -->
    <ImageButton
        android:id="@+id/forward"
        android:layout_above="@id/whitespace"
        android:layout_centerHorizontal="true"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/forward"
        android:background="@drawable/car_control_button"
        style="?android:attr/borderlessButtonStyle"/>

    <!-- Backward button -->
    <ImageButton
        android:id="@+id/backward"
        android:layout_below="@id/whitespace"
        android:layout_centerHorizontal="true"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/backward"
        android:background="@drawable/car_control_button"
        style="?android:attr/borderlessButtonStyle"/>

    <!-- Left button -->
    <ImageButton
        android:id="@+id/left"
        android:layout_toLeftOf="@id/whitespace"
        android:layout_centerVertical="true"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/left"
        android:background="@drawable/car_control_button"
        style="?android:attr/borderlessButtonStyle"/>

    <!-- Right button -->
    <ImageButton
        android:id="@+id/right"
        android:layout_toRightOf="@id/whitespace"
        android:layout_centerVertical="true"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/right"
        android:background="@drawable/car_control_button"
        style="?android:attr/borderlessButtonStyle"/>

</RelativeLayout>